name: 01 - Building Blocks
on:
  - push
  - workflow_dispatch

jobs:
  job-1:
    name: Echo Hello
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Hello, World!"

  job-2:
    name: Failure Job
    runs-on: ubuntu-latest
    steps:
      - name: Success step
        run: |
          echo "I will succeed"
          exit 0

      - name: Say goodbye
        run: echo "Goodbye!"
